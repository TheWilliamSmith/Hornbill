meta {
  name: Create Sell Execution
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/crypto/executions
  body: json
  auth: inherit
}

body:json {
  {
    "positionId": "{{positionId}}",
    "quantitySold": 0.002,
    "sellPrice": 120000,
    "fees": 1.5
  }
}
