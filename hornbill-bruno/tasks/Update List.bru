meta {
  name: Update List
  type: http
  seq: 8
}

patch {
  url: {{baseUrl}}/tasks/lists/{{listId}}
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Courses (updated)"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
