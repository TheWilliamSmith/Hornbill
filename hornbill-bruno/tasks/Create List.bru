meta {
  name: Create List
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/tasks/workspaces/{{workspaceId}}/lists
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Courses"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
