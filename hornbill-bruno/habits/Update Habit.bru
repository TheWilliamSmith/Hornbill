meta {
  name: Update Habit
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/habits/{{habitId}}
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Sport (updated)",
    "targetPerWeek": 4
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
