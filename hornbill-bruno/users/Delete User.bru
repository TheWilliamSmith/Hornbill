meta {
  name: Delete User
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/users/me
  body: json
  auth: inherit
}

body:json {
  {
    "password": "MyPassword123!"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
