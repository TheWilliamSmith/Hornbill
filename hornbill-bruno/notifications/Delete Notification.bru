meta {
  name: Delete Notification
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/notifications/:id
  body: none
  auth: inherit
}

params:path {
  id: {{notificationId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
